services:
  - type: web
    name: ai-tutor
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn aitutor.wsgi:application"

    envVars:
      - key: DEBUG
        value: "False"
      - key: PYTHON_VERSION
        value: "3.9.0"
